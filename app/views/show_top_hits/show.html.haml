.row
  .col-md-12
    .stories
      %h2 Recent Top Hits
      %table.table
        - @stories.each do |tophit|
          %tr
            = render :partial => 'stories/tophit', :locals => { :tophit => tophit }
    = paginate @stories
